# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddToCartInput {
  productId: String!
  quantity: Int!
}

type Cart {
  id: ID!
  items: [CartItem!]!
  status: String!
  subtotal: Float!
}

type CartItem {
  id: ID!
  product: Product!
  quantity: Int!
  unitPrice: Float!
}

type Category {
  createdAt: String!
  id: ID!
  name: String!
  slug: String!
}

type Coupon {
  active: Boolean!
  code: String!
  endDate: DateTime
  id: ID!
  minTotal: Float!
  startDate: DateTime
  type: String!
  value: Float!
}

input CreateProductInput {
  brand: String
  categoryId: String
  compareAtPrice: Float
  description: String
  isActive: Boolean!
  model: String
  name: String!
  price: Float!
  sku: String!
  specs: JSONObject
  stock: Int!
}

type DailyRevenue {
  date: String!
  orders: Int!
  revenue: Float!
}

type DatabaseStatus {
  connected: Boolean!
  databaseUrl: String!
  tables: [TableStatus!]!
  version: Int!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input GuestCartItemInput {
  productId: String!
  quantity: Int!
}

"""
The `JSONObject` scalar type represents JSON objects as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSONObject @specifiedBy(url: "http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf")

input MergeCartInput {
  items: [GuestCartItemInput!]!
}

type Mutation {
  addToCart(input: AddToCartInput!): Cart!
  adminSeedDemoData: JSONObject!
  adminUpdateStock(input: UpdateStockInput!): Product!
  createCategory(name: String!): Category!
  createOrderFromCart(couponCode: String, shippingAddress: JSONObject!): Order!
  createProduct(input: CreateProductInput!): Product!
  mergeCart(input: MergeCartInput!): Cart!
  removeCartItem(itemId: String!): Cart!
  toggleWishlist(input: ToggleWishlistInput!): [WishlistItem!]!
  updateCartItem(input: UpdateCartItemInput!): Cart!
}

type Order {
  createdAt: String!
  id: ID!
  items: [OrderItem!]!
  paymentStatus: String!
  shipping: Float!
  shippingAddress: JSONObject!
  status: String!
  subtotal: Float!
  total: Float!
}

type OrderItem {
  id: ID!
  productId: ID!
  quantity: Int!
  unitPrice: Float!
}

type PageInfo {
  endCursor: String
  hasNextPage: Boolean!
}

input PaginationInput {
  after: String
  first: Int
}

type Product {
  brand: String
  category: String
  compareAtPrice: Float
  createdAt: String!
  description: String
  id: ID!
  images: [ProductImage!]!
  isActive: Boolean!
  isLowStock: Boolean!
  model: String
  name: String!
  outOfStock: Boolean!
  price: Float!
  sku: String!
  slug: String!
  specs: JSONObject
  stock: Float!
  variants: [ProductVariant!]!
}

type ProductEdge {
  cursor: String!
  node: Product!
}

type ProductImage {
  id: ID!
  position: Int!
  url: String!
}

type ProductVariant {
  id: ID!
  name: String!
  price: Float
  stock: Int
  value: String!
}

type ProductsConnection {
  edges: [ProductEdge!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

input ProductsFilterInput {
  brand: String
  categorySlug: String
  inStockOnly: Boolean
  isActive: Boolean
  limit: Int
  maxPrice: Float
  minPrice: Float
  model: String
  page: Int
  search: String
}

type Query {
  adminCustomerDetail(id: ID!): JSONObject!
  adminCustomers(search: String): [User!]!
  adminDatabaseStatus: DatabaseStatus!
  adminLowStockAlerts(threshold: Int = 3): [Product!]!
  adminOrders(filter: JSONObject): [JSONObject!]!
  adminProducts(filter: ProductsFilterInput): [Product!]!
  adminSalesSummary(dateRange: String! = "30d"): SalesSummary!
  adminStockMovements(filter: StockMovementFilterInput): [StockMovement!]!
  cart: Cart!
  categories: [Category!]!
  categoryBySlug(slug: String!): Category!
  me: User
  order(orderId: String!): Order!
  orders: [Order!]!
  productBySlug(slug: String!): Product!
  products(filter: ProductsFilterInput): [Product!]!
  productsConnection(filter: ProductsFilterInput, pagination: PaginationInput, sort: String): ProductsConnection!
  relatedProducts(limit: Float = 12, productId: ID!): [Product!]!
  validateCoupon(code: String!, total: Float!): Coupon!
  wishlist: [WishlistItem!]!
}

type SalesSummary {
  avgOrderValue: Float!
  revenueByDay: [DailyRevenue!]!
  topProducts: [TopProductSales!]!
  totalOrders: Int!
  totalRevenue: Float!
}

type StockMovement {
  createdAt: String!
  id: ID!
  productId: ID!
  qty: Int!
  reason: String
  type: StockMovementType!
}

input StockMovementFilterInput {
  endDate: String
  productId: ID
  startDate: String
}

enum StockMovementType {
  ADJUST
  IN
  OUT
}

type StockUpdate {
  productId: ID!
  stock: Int!
  updatedAt: String!
}

type Subscription {
  stockUpdated(productIds: [String!]): StockUpdate!
}

type TableStatus {
  lastUpdate: String
  name: String!
  rowCount: Int!
}

input ToggleWishlistInput {
  productId: String!
}

type TopProductSales {
  name: String!
  productId: String!
  revenue: Float!
  unitsSold: Int!
}

input UpdateCartItemInput {
  itemId: String!
  quantity: Int!
}

input UpdateStockInput {
  productId: ID!
  qty: Int!
  reason: String
  type: StockMovementType!
}

type User {
  createdAt: String!
  email: String!
  fullName: String
  id: ID!
  phone: String
  role: String
  totalOrders: Int!
  totalSpent: Float!
}

type WishlistItem {
  createdAt: DateTime!
  id: ID!
  product: Product!
  productId: ID!
  userId: ID!
}